trigger:
  branches:
    include:
    - develop
stages:
- stage: __default
  jobs:
  - job: Job
    pool:
      vmImage: 'ubuntu-latest'

    - task: SynopsysDetectTask@6
      inputs:
        BlackDuckService: 'blackduck1'
        DetectVersion: 'latest'